import { createSlice } from "@reduxjs/toolkit";

const loginSlice = createSlice({
    name: "mainPage",
    initialState: {examMark: null},
    reducers: { 

    },
})

export default loginSlice.reducer